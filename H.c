#include <stdio.h>

int k,p;

int main(){
    scanf("%d %d",&k, &p);

    int arrCombinaciones[1000];

    for(int i=0; i<p; i++){
        scanf("%d", &arrCombinaciones[i]);
    }

    while(k--){
        scanf("%d", &k);
    }















    return 0;
}